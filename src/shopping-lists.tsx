import { Icon, List } from "@raycast/api";

// Platzhalter. Wird in einer späteren Task durch die echte Implementierung ersetzt.
export default function Placeholder() {
  return (
    <List>
      <List.EmptyView icon={Icon.Hammer} title="Not implemented yet" description="This command is still being built." />
    </List>
  );
}
